import { maxDimensions } from "./../../../../misc/storyHelpers";

export const mdCardContainer = (component) => maxDimensions('464px', '144px', component)
export const smCardContainer = (component) => maxDimensions('225px', '144px', component)